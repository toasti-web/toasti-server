'use strict';

/**
 *  non-month-view-placeholder controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::non-month-view-placeholder.non-month-view-placeholder');
