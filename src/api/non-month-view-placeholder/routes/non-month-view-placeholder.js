'use strict';

/**
 * non-month-view-placeholder router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::non-month-view-placeholder.non-month-view-placeholder');
