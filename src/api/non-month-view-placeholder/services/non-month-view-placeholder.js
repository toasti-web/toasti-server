'use strict';

/**
 * non-month-view-placeholder service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::non-month-view-placeholder.non-month-view-placeholder');
